---
- name: Wait for system ready
  hosts: 'moonbase'
  gather_facts: false
  become: true
  tasks:
    - name: Wait for nodes to become reachable
      wait_for_connection:

- name: Validator Full Setup
  hosts: 'moonbase'
  become: true
  gather_facts: false
  roles:
    - moonbase
