---
- name: Wait for system ready
  hosts: 'chachacha'
  gather_facts: false
  become: true
  tasks:
    - name: Wait for nodes to become reachable
      wait_for_connection:

- name: Validator Full Setup
  hosts: 'chachacha'
  become: true
  gather_facts: false
  roles:
    - chachacha
